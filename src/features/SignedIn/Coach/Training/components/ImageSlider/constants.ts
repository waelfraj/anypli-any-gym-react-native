import {
  WINDOW_HEIGHT,
  WINDOW_WIDTH
} from '../../../../../../utils/constants/layout';

export const AUTOPLAY_INTERVAL = 4000;
export const SLIDER_WIDTH = WINDOW_WIDTH;
export const ITEM_WIDTH = WINDOW_WIDTH - 70;
export const FIRST_ITEM = 1;
export const SLIDER_HEIGHT = WINDOW_HEIGHT / 4;
