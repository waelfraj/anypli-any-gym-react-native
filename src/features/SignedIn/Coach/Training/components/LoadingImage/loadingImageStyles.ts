import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  image: {
    borderRadius: 10,
    width: '100%',
    height: '100%',
    resizeMode: 'contain'
  },
  container: {
    height: '100%',
    justifyContent: 'center'
  }
});
